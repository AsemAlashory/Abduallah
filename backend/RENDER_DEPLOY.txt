Render backend deploy package - Phase 2 fast response

Use on Render Web Service:
Build command: pip install -r requirements.txt
Start command: uvicorn app.main:app --host 0.0.0.0 --port $PORT
Health URL after deploy: /api/health

This package reduces Phase 2 analyze response size for Hostinger/Render.
